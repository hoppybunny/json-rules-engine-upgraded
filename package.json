{
  "name": "json-rules-engine-upgraded-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo root for json-rules-engine-upgraded",
  "license": "MIT",
  "author": "clara",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test:run",
    "bench": "pnpm -r bench",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "changeset": "changeset",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "2.27.9",
    "@types/node": "20.14.2",
    "tsup": "8.3.0",
    "typescript": "5.5.4",
    "vitest": "2.1.1",
    "tinybench": "3.0.6",
    "publint": "0.2.10"
  }
}
